\documentclass[$if(sidenotes)$sidenotes,$endif$$if(twoside)$twoside,$endif$]{polytextum}

% Metadata
$if(formattedtitle)$
\title{$formattedtitle$}
$else$
\title{$title$}
$endif$

$if(author)$
\author{$author$}
$else$
\author{Aramis Razzaghipour}
$endif$

$if(date)$
\date{$date$}
$else$
\date{\today}
$endif$

\hypersetup{
  $if(author)$
  pdfauthor={$author$},
  $else$
  pdfauthor={Aramis Razzaghipour},
  $endif$
  pdftitle={$title$},
  pdfsubject={$title$},
  $if(author)$
  pdfkeywords={$author$, $title$},
  $else$
  pdfkeywords={Aramis Razzaghipour, $title$},
  $endif$
}

\begin{document}

\pagenumbering{roman}
\maketitlepage
  $if(author1)$
    {$author1$}{$author2$}
  $else$
    {$if(author)$$author$$else$Aramis Razzaghipour$endif$}{}
  $endif$
  $if(title1)$
    {$title1$}{$title2$}
  $else$
    {$title$}{}
  $endif$

\cleardoublepage

\pagenumbering{arabic}
\setcounter{page}{1}

$body$

% Print bibliography
\cleardoublepage
\printbibliography[heading=bibintoc]

% Leave one or two pages blank
\cleardoublepage
\pagestyle{empty}
\null

\end{document}
